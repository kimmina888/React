import React from "react";

let list.defaultProps = {
    img,
    name,
    post
}

var lists = (list.img, list.name, list.post)=>{
    return (
        <div>
            <table>
                <tr><td></td></tr>
            </table>
        </div>
    )
}
export 